{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "carla": {
      "type": "stdio",
      "command": "node",
      "args": ["bin/carla-agentic-tools.js"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
